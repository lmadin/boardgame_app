services:
  - type: web
    name: boardgame-app
    runtime: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: boardgame-data
      mountPath: /var/data
      sizeGB: 1
    envVars:
      - key: DATA_DIR
        value: /var/data
